# 기본 모델 캐시 100회 검증

2026-09-14 17:12:49–17:15:29 KST, 약 160.2초.

- 대상: 사용자가 실행한 codexy, http://127.0.0.1:8787/v1/responses
- 요청 model 필드 생략. 서버 기본 모델 사용, 100개 응답 모두 gpt-5.6-luna 확인.
- 순차 100회, 별도의 대기나 실패 재시도 없음.
- 동일 입력·본문·session-id·prompt_cache_key 유지.
- 기존 검증에서 이미 사용한 캐시 키로 시작한 warm-cache 실험.
- 미스 정의: 정상 완료 응답의 cached_tokens=0. HTTP/전송 오류는 별도 집계.

| 결과 | 횟수 |
| --- | ---: |
| 캐시 적중 | 100 |
| 캐시 미스 | 0 |
| HTTP/전송 오류 | 0 |

100회 모두 input_tokens=4574, cached_tokens=3840.
이번 표본의 요청 단위 미스율은 0%입니다. 모든 입력 토큰이 캐시된다는 의미나
다른 시간·부하에서도 미스가 없다는 보장은 아닙니다.

원시 진단 결과: [cache-100-results.json](cache-100-results.json).
응답 ID·시간·사용량만 보존하며 토큰 자격 증명과 답변 원문은 포함하지 않습니다.
소스와 Cargo 파일의 SHA-256을 비교해 조사 시작 때와 동일함을 확인했습니다.
